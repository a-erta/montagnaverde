<li class="nav-header"><%= t(:title) %></li>
<% unless cart.line_items.empty? %>
<table>

<%= render(cart.line_items) %>

<tr>
<td colspan="2" >Total</td>
<td><%= number_to_currency(cart.total_price, :unit => "€") %></td>
</tr>
<tr>
<td>
<%= button_to t(:checkout), new_order_path, :class => 'btn btn-mini', :method => :get %>
</td>
<td>
<%= button_to t(:empty), cart, :class => 'btn btn-mini', :method => :delete,
:data => { :confirm => 'Are you sure?' } %>
</td>
</tr>
</table>
<% end %>